
---

**Java & Spring Boot**

1. Explain the Spring Bean lifecycle and the difference between `@Component`, `@Service`, and `@Repository`.
2. What is dependency injection and how does Spring implement it? Difference between constructor vs field injection?
3. How do you handle transactions in Spring Boot? What does `@Transactional` actually do under the hood?
4. Explain Spring Boot auto-configuration. How would you override or exclude it?
5. How do you secure a REST API in Spring Boot? Walk me through JWT-based authentication.

**SQL**

6. Write a query using window functions — e.g. rank employees by salary within each department.
7. Explain the difference between `INNER JOIN`, `LEFT JOIN`, and a subquery. When would you use each?
8. How do you diagnose and fix a slow query? What tools and steps do you use?
9. What is the difference between a clustered and non-clustered index?
10. Explain database normalisation up to 3NF and when you'd deliberately denormalise.

**AWS**

11. Explain the difference between EC2, ECS, and Lambda — when would you choose each?
12. How does IAM work? Explain roles, policies, and the principle of least privilege.
13. What is the difference between SQS and SNS? Describe a use case for each.
14. How would you design a highly available, fault-tolerant application on AWS?
15. What is a VPC? Explain subnets, security groups, and NACLs.

**Python**

16. What are Python decorators? Write one from scratch.
17. Explain the GIL. How do you achieve true parallelism in Python?
18. What's the difference between `multiprocessing`, `threading`, and `asyncio`? When would you use each?
19. How do you structure a Python project for production? (packaging, dependency management, testing)

**Consulting / System Design**

20. Design a scalable REST API that ingests high-volume data, stores it in a database, and exposes it to clients — using the AWS/Java/Python stack. Walk me through your decisions.

---

Question 20 is almost certain to appear in some form. Questions 3, 6, 12, and 14 are also near-guaranteed for this stack. Make sure you can whiteboard SQL on the spot and talk through AWS architecture decisions with cost and scalability trade-offs in mind, since that's the consulting angle they'll care about.